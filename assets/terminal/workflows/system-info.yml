id: system-info
title: System Info
tags: ["system"]
description: Show basic system info
inputs: []
run:
  mode: action
  action:
    name: shell
    params:
      command: "powershell -NoProfile -Command \"Get-ComputerInfo | Select-Object CsName,OsName,OsVersion | Format-List\""
