{
  "name": "windows-ai",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "echo \"(optional) build all workspaces\"",
    "test": "npm test -w apps/actions-api"
  }
}