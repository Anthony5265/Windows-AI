# windows-ai-tray

Minimal Electron tray for Windows AI.

## Requirements

- Node.js 18+ / Electron 18+ (provides global `fetch`; the app polyfills with `node-fetch` when needed.)

## Quick start

```powershell
cd windows-ai-tray
npm install
npm start
```
