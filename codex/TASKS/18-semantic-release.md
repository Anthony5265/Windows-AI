# Task 18 — semantic-release


Configure semantic-release for automated versioning and changelog generation.
Acceptance:
- On main, semantic-release publishes a new version and changelog when rules match.
