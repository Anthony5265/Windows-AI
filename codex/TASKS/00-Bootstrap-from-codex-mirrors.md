# Task 00 — Bootstrap-from-codex-mirrors


Copy mirrors into proper repo paths:

- Move `codex/openapi/windows-ai.yaml` → `openapi/windows-ai.yaml`.
- Copy `codex/ROOT_FILES/{.editorconfig,.gitattributes,.gitignore}` to repo root.
- Copy `codex/TEMPLATES/*` to `.github/` (issue/PR templates).
- Optionally copy `codex/GITHUB_WORKFLOWS/*` to `.github/workflows/`.
- Commit: `chore(bootstrap): apply codex mirrors`.
Acceptance:
- Files are in correct locations and visible on GitHub.
