# Installers — Runtimes

The installer runs `scripts/runtime-bootstrap.ps1` to download and place Node.js and Python embeddable under `C:\Program Files\Windows AI\runtime\`.
Start scripts prefer bundled runtimes and fall back to system Node/Python.
