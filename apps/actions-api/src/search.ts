import { ValidationError } from './errors';export interface SearchRequest {  query: string;  documents?: Record<string, string>;}function tokenize(text: string): Set<string> {  return new Set(text.toLowerCase().split(/\W+/).filter(Boolean));}export function searchDocuments(req: SearchRequest) {  const query = req.query?.trim();  if (!query) throw new ValidationError('Missing query');  const docs = req.documents || {};  const qTokens = tokenize(query);  const scores: Array<{ id: string; score: number }> = [];  for (const [id, text] of Object.entries(docs)) {    const docTokens = tokenize(text);    const score = [...qTokens].filter((t) => docTokens.has(t)).length;    if (score > 0) scores.push({ id, score });  }  scores.sort((a, b) => b.score - a.score);  return scores.map((s) => s.id);}