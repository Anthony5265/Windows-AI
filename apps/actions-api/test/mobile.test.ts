import { test } from 'node:test';import assert from 'node:assert';import { app } from '../src/index';async function startServer() {  const server = app.listen(0);  await new Promise<void>((resolve) => server.once('listening', () => resolve()));  const address = server.address();  if (typeof address === 'string' || !address) {    throw new Error('Failed to get server address');  }  return { server, port: address.port };}test('pairing returns token', async () => {  const { server, port } = await startServer();  const res = await fetch(`http://localhost:${port}/api/mobile/pair`, {    method: 'POST',    headers: { 'content-type': 'application/json' },    body: JSON.stringify({ deviceId: 'device1' })  });  assert.strictEqual(res.status, 200);  const body = await res.json();  assert.strictEqual(body.ok, true);  assert.ok(body.token);  await new Promise((resolve) => server.close(resolve));});test('remote command executes with token', async () => {  const { server, port } = await startServer();  const pairRes = await fetch(`http://localhost:${port}/api/mobile/pair`, {    method: 'POST',    headers: { 'content-type': 'application/json' },    body: JSON.stringify({ deviceId: 'device2' })  });  const token = (await pairRes.json()).token;  const cmdRes = await fetch(`http://localhost:${port}/api/mobile/command`, {    method: 'POST',    headers: { 'content-type': 'application/json' },    body: JSON.stringify({ token, action: 'get_system_info' })  });  assert.strictEqual(cmdRes.status, 200);  const body = await cmdRes.json();  assert.strictEqual(body.ok, true);  assert.ok(body.result.platform);  await new Promise((resolve) => server.close(resolve));});test('remote command rejects invalid token', async () => {  const { server, port } = await startServer();  const cmdRes = await fetch(`http://localhost:${port}/api/mobile/command`, {    method: 'POST',    headers: { 'content-type': 'application/json' },    body: JSON.stringify({ token: 'bad', action: 'get_system_info' })  });  assert.strictEqual(cmdRes.status, 400);  const body = await cmdRes.json();  assert.strictEqual(body.ok, false);  await new Promise((resolve) => server.close(resolve));});
