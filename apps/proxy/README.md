# Proxy

This proxy forwards requests to available local or remote model hosts.

## Installation

Install dependencies:

```bash
cd apps/proxy
npm install
```

## Usage

Start the proxy server:

```bash
npm start
```
