# Task 02 — Installer-and-Services


Create PowerShell installers (install.ps1/uninstall.ps1), register services (node-windows, NSSM), mkcert TLS, firewall rules.
Acceptance:
- install/uninstall scripts run without error on Windows 11.
