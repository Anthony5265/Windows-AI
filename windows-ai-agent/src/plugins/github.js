import fetch from 'node-fetch';export async function dispatchWorkflow(owner, repo, workflow_id, ref='main', inputs={}){  const token = process.env.GITHUB_TOKEN || process.env.GH_TOKEN;  if (!token) throw new Error('GITHUB_TOKEN not set');  const url = `https://api.github.com/repos/${owner}/${repo}/actions/workflows/${workflow_id}/dispatches`;  const r = await fetch(url, {    method: 'POST',    headers: {      'Authorization': `Bearer ${token}`,      'Accept': 'application/vnd.github+json',      'X-GitHub-Api-Version': '2022-11-28'    },    body: JSON.stringify({ ref, inputs })  });  if (!r.ok) {    const t = await r.text().catch(()=>'');    throw new Error(`GitHub dispatch failed: ${r.status} ${r.statusText} ${t}`);  }  return { status:'ok' };}