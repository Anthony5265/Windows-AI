import fetch from 'node-fetch';export async function ask(prompt){const model=process.env.OLLAMA_MODEL||'llama3.1';const r=await fetch('http://localhost:11434/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model,messages:[{role:'user',content:prompt}],stream:false})});if(!r.ok){const t=await r.text().catch(()=> '');throw new Error(`Ollama request failed: ${r.status} ${r.statusText} ${t}`);}const j=await r.json();return j.message?.content?.trim()||JSON.stringify(j);}
