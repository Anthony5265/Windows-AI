# Windows AI — Master Spec v0.5 (Windows 11)

**Scope of this update (the “other 3”):**
1) **Sample Assets Pack** — ship default Terminal **themes**, **keysets**, and **workflows**.
2) **AgentHub endpoint** for workflows: `POST /workflows/run` (detailed contract & examples).
3) **Expanded Test Suite** — Unicode width checks (xterm) + node-pty PowerShell `SystemRoot` env guard + workflow E2E pack.

This builds on v0.4 (Terminal & Workflows module).
