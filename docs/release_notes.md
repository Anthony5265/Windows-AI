# Release Notes

## Installer Automation
- The WindowsAI installer now automatically runs `install\install.ps1` with administrator rights after you close the GUI.
- Windows may display a User Account Control prompt; approve it to allow required services and integrations to be configured.
