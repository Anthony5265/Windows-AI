const API_BASE = 'https://example.com/api';async function loginRequest(username, password) {  const res = await fetch(`${API_BASE}/login`, {    method: 'POST',    headers: { 'Content-Type': 'application/json' },    body: JSON.stringify({ username, password }),  });  if (!res.ok) {    throw new Error('Login failed');  }  return true;}async function pairRequest(deviceId) {  const res = await fetch(`${API_BASE}/pair`, {    method: 'POST',    headers: { 'Content-Type': 'application/json' },    body: JSON.stringify({ deviceId }),  });  if (!res.ok) {    throw new Error('Pairing failed');  }  const data = await res.json();  return data.token;}module.exports = { loginRequest, pairRequest };